# 🚀 Швидкий старт

Покрокова інструкція для запуску AI чат-бота з CrewAI та OpenAI.

## Крок 1: Клонування репозиторію

```bash
git clone <your-repository-url>
cd crewai-chatbot
```

## Крок 2: Встановлення Python залежностей

Переконайтеся що у вас встановлено Python 3.10 або новіше:

```bash
python --version
```

Встановіть залежності:

```bash
pip install -r requirements.txt
```

Або встановіть окремо:

```bash
pip install crewai crewai-tools openai python-dotenv langchain langchain-openai streamlit
```

## Крок 3: Отримання OpenAI API ключа

1. Перейдіть на [platform.openai.com](https://platform.openai.com)
2. Створіть акаунт або увійдіть
3. Перейдіть в розділ **API Keys**
4. Натисніть **Create new secret key**
5. Скопіюйте ключ (він показується тільки один раз!)

## Крок 4: Налаштування змінних оточення

Створіть файл `.env` в корені проєкту:

```bash
cp .env.example .env
```

Або створіть вручну:

```bash
nano .env
```

Додайте ваш API ключ:

```env
# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Specify model (default is gpt-4o-mini)
OPENAI_MODEL_NAME=gpt-4o-mini

# ChromaDB requires the same key
CHROMA_OPENAI_API_KEY=your_openai_api_key_here
```

**⚠️ ВАЖЛИВО:** Ніколи не комітьте файл `.env` в git! Він має бути в `.gitignore`.

## Крок 5: Перевірка доступу до моделей OpenAI

Переконайтеся що ваш акаунт має доступ до потрібних моделей:

1. Перейдіть на [platform.openai.com/settings/organization/limits](https://platform.openai.com/settings/organization/limits)
2. Перевірте що доступні:
   - `gpt-4o-mini` (для чат-бота)
   - `text-embedding-3-small` (для пам'яті)

Якщо моделі недоступні:
- Додайте платіжний метод в налаштуваннях
- Поповніть баланс (мінімум $5)

## Крок 6: Запуск консольної версії

Для швидкого тесту запустіть консольну версію:

```bash
python main_short_term_only.py
```

Ви побачите:

```
🤖 Ініціалізація Conversation Agent...
✅ Агент готовий до роботи!
💡 Пам'ять увімкнено - агент запам'ятає контекст розмови

Ти: 
```

Введіть повідомлення і натисніть Enter. Для виходу введіть `exit`.

### Тест пам'яті:

```
Ти: Моє ім'я Антон
🤖 Агент: Привіт, Антоне! Радий познайомитися!

Ти: Як мене звати?
🤖 Агент: Тебе звати Антон!
```

## Крок 7: Запуск веб-інтерфейсу (рекомендовано)

Запустіть Streamlit застосунок:

```bash
streamlit run app_v2.py
```

Streamlit автоматично:
- Відкриє браузер
- Запустить сервер на `http://localhost:8501`

### Особливості веб-інтерфейсу:

✅ Красивий chat UI  
✅ Автоматичне збереження історії  
✅ Кнопка "Очистити історію"  
✅ Індикатор завантаження  

## 🎯 Типові проблеми та рішення

### Помилка: "Error code: 403 - model not found"

**Проблема:** Ваш OpenAI проєкт не має доступу до моделі.

**Рішення:**
1. Перевірте що ви додали платіжний метод
2. Поповніть баланс
3. Дочекайтеся активації моделей (може зайняти кілька хвилин)

### Помилка: "OPENAI_API_KEY not found"

**Проблема:** Файл `.env` не створений або ключ не вказаний.

**Рішення:**
1. Створіть файл `.env` в корені проєкту
2. Додайте `OPENAI_API_KEY=ваш_ключ`
3. Перезапустіть програму

### Помилка: "Failed to initialize embedder"

**Проблема:** Проблема з налаштуванням embeddings для пам'яті.

**Рішення:**
1. Додайте `CHROMA_OPENAI_API_KEY` в `.env` (той самий ключ)
2. Перевірте доступ до моделі `text-embedding-3-small`

### Агент не пам'ятає контекст

**Проблема:** Використовуєте старий файл `app.py` замість `app_v2.py`.

**Рішення:**
- Запускайте саме `app_v2.py`: `streamlit run app_v2.py`
- Або скопіюйте вміст: `cp app_v2.py app.py`

### Streamlit показує помилку "Address already in use"

**Проблема:** Порт 8501 вже зайнятий.

**Рішення:**
```bash
streamlit run app_v2.py --server.port 8502
```

## 📊 GitHub Codespaces

Якщо використовуєте GitHub Codespaces:

1. Усі команди працюють так само
2. Streamlit автоматично форвардить порти
3. Пам'ять зберігається в `/home/codespace/.local/share/crewai-chatbot/`

## 🧹 Очищення пам'яті

Якщо потрібно очистити всю пам'ять (наприклад, після тестування):

```bash
rm -rf ~/.local/share/crewai-chatbot/*
```

Або в GitHub Codespaces:

```bash
rm -rf /home/codespace/.local/share/crewai-chatbot/*
```

## ✅ Що далі?

Після успішного запуску:

1. Протестуйте різні сценарії розмов
2. Поекспериментуйте з налаштуваннями (temperature, model)
3. Додайте нових агентів (Image, Audio)
4. Задеплойте на Streamlit Cloud

Детальніше про архітектуру та розробку дивіться в [README.md](README.md).

## 🆘 Потрібна допомога?

- Перевірте [Issues](https://github.com/your-repo/issues)
- Читайте [CrewAI Documentation](https://docs.crewai.com)
- Перевіряйте [OpenAI API Status](https://status.openai.com)

---

**Готово! 🎉 Насолоджуйтесь своїм AI чат-ботом!**
