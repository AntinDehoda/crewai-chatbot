# 🤖 AI Чат-бот з CrewAI + RAG

Розумний чат-бот з короткостроковою пам'яттю та RAG (Retrieval-Augmented Generation) для роботи з PDF документами. Побудований на базі CrewAI та OpenAI GPT-4o-mini.

## ✨ Особливості

- **RAG для PDF документів**: Завантажуйте PDF файли та отримуйте відповіді на основі їх змісту
- **Short-term Memory**: Агент пам'ятає контекст розмови в межах сесії
- **Два векторні сховища**: ChromaDB (за замовчуванням) та FAISS для різних потреб
- **RAG Evaluation**: Інструменти для оцінки та порівняння RAG систем (ragas metrics)
- **OpenAI GPT-4o-mini**: Швидка та економічна модель для чат-бота
- **Три інтерфейси**:
  - Веб-застосунок з PDF upload (`webapp.py`) - **Рекомендовано**
  - Консольний з RAG (`main_with_rag.py`)
  - Базовий консольний (`main_short_term_only.py`)
- **Управління документами**: Завантаження, перегляд та видалення PDF файлів
- **Інтелектуальний пошук**: Агент автоматично шукає інформацію в документах при необхідності
- **Порівняння продуктивності**: Benchmark інструменти для порівняння vector stores



## 🏗️ Архітектура

```
crewai-chatbot/
├── agents/
│   └── conversation_agent.py    # Агент з RAG підтримкою
├── tools/
│   └── rag_tool.py              # RAG інструмент для пошуку в документах
├── utils/
│   ├── pdf_processor.py         # Обробка PDF (завантаження, chunking)
│   └── vector_store.py          # Управління векторною базою даних
├── webapp.py                    # Веб-інтерфейс з PDF upload (РЕКОМЕНДОВАНО)
├── main_with_rag.py             # Консольний інтерфейс з RAG
├── main_short_term_only.py      # Базовий консольний інтерфейс
├── test_scenarios.py            # Тестові сценарії
├── requirements.txt             # Python залежності
└── .env                         # API ключі (не включено в репозиторій)
```

### Компоненти

**Conversation Agent** (`agents/conversation_agent.py`)
- Роль: Дружній асистент для розмови з доступом до документів
- LLM: OpenAI GPT-4o-mini
- Temperature: 0.7 (баланс між креативністю та послідовністю)
- Tools: RAG Search Tool для пошуку в PDF

**RAG System**
- **PDF Processor**: Завантаження та розбиття PDF на chunk'и (1000 символів)
- **Vector Stores**: ChromaDB (за замовчуванням) або FAISS для зберігання embeddings
- **RAG Tool**: Пошук релевантних фрагментів за запитом користувача
- **Embeddings**: OpenAI text-embedding-3-small
- **Evaluation**: Ragas metrics для оцінки якості RAG

**Memory System**
- Short-term memory для контексту розмови (ChromaDB)
- Окреме векторне сховище для документів
- Зберігає контекст розмови та документи між сесіями

**Web Interface** (`webapp.py`)
- Streamlit chat UI з sidebar для документів
- PDF file uploader з автоматичною обробкою
- Відображення завантажених документів
- Управління документами (видалення окремих файлів)
- Статистика векторної бази
- Session state для збереження Crew, історії та документів

## 📋 Вимоги

- Python 3.10+
- OpenAI API ключ
- Доступ до моделей:
  - `gpt-4o-mini`
  - `text-embedding-3-small`

## 🚀 Швидкий старт

Дивись [QUICKSTART.md](QUICKSTART.md) для детальних інструкцій з налаштування та запуску.

## 💡 Використання

### 🌟 Веб-інтерфейс з RAG (рекомендовано)

```bash
streamlit run webapp.py
```

Відкриється браузер з чат-інтерфейсом та sidebar для управління документами.

**Як використовувати RAG:**

1. **Завантаження PDF**:
   - У sidebar натисніть "Виберіть PDF файл"
   - Оберіть PDF документ
   - Натисніть "📤 Завантажити та обробити"
   - Дочекайтесь підтвердження успішного завантаження

2. **Розмова з документами**:
   ```
   Користувач: Що сказано в документі про ціни?
   Бот: [шукає в PDF та надає відповідь з посиланням на джерело]

   Користувач: Знайди інформацію про технічні характеристики
   Бот: [витягує релевантні фрагменти з документа]
   ```

3. **Управління документами**:
   - Перегляд завантажених файлів у sidebar
   - Видалення окремих документів кнопкою 🗑️
   - Очищення всієї бази документів
   - Статистика: кількість фрагментів та документів

**Особливості:**
- Агент **автоматично** вирішує коли шукати в документах
- Пам'ять розмови + RAG працюють разом
- Підтримка багатьох документів одночасно
- Візуальний індикатор обробки

### 💻 Консольний інтерфейс з RAG

```bash
python main_with_rag.py
```

Консольний інтерфейс з підтримкою всіх RAG функцій.

**Доступні команди:**
```
chat           - Розмова з ботом
upload <path>  - Завантажити PDF файл
list           - Показати завантажені документи
delete <name>  - Видалити документ
clear-docs     - Очистити всі документи
clear-chat     - Очистити історію чату
stats          - Показати статистику
help           - Показати меню команд
exit           - Вихід
```

**Приклад використання:**
```bash
📝 Команда: upload /path/to/document.pdf
# Документ завантажується та обробляється

📝 Команда: chat
# Входимо в режим чату

💬 Ти: Що написано в документі про гарантію?
🤖 Бот: [відповідь на основі PDF]

💬 Ти: exit
# Повертаємося до меню команд
```

### 📝 Базовий консольний інтерфейс

```bash
python main_short_term_only.py
```

Простий інтерфейс без RAG функціональності. Для виходу: `exit`, `quit`, `вихід`, `вийти`.

## 🧪 Тестування пам'яті

Щоб перевірити що агент запам'ятовує контекст:

```
Ти: Моє ім'я Олександр
Агент: Привіт, Олександр! ...

Ти: Який мій улюблений колір синій
Агент: Добре запам'ятав ...

Ти: Як мене звати і який мій улюблений колір?
Агент: Тебе звати Олександр і твій улюблений колір синій!
```

## 🔧 Конфігурація

### Зміна моделі

У файлі `agents/conversation_agent.py`:

```python
llm = ChatOpenAI(
    model="gpt-4o-mini",  # Змініть на іншу модель
    temperature=0.7,       # Налаштуйте креативність (0.0-1.0)
)
```

### Кількість повідомлень у контексті

У файлі `webapp.py`:

```python
def format_conversation_context(messages, last_n=5):  # Змініть 5 на потрібну кількість
```

### Вимкнення verbose режиму

У консольній версії змініть:

```python
crew = Crew(
    verbose=False,  # True для детальних логів
    ...
)
```

## 🗂️ Управління пам'яттю

### Очищення пам'яті

Пам'ять зберігається в:
```
~/.local/share/crewai-chatbot/
```

Для повного очищення:

```bash
rm -rf ~/.local/share/crewai-chatbot/*
```

Або використовуйте кнопку "Очистити історію" у веб-інтерфейсі.

## 🚢 Deployment

### Streamlit Cloud (безкоштовно)

1. Завантажте проєкт на GitHub
2. Підключіться до [share.streamlit.io](https://share.streamlit.io)
3. Вкажіть репозиторій та `webapp.py` як головний файл
4. Додайте `OPENAI_API_KEY` в Secrets

Детальніше: [Streamlit Cloud Documentation](https://docs.streamlit.io/streamlit-community-cloud)

## 🧪 Тестування RAG

Щоб перевірити що RAG працює правильно:

1. **Завантажте тестовий PDF** (наприклад, технічну документацію)
2. **Задайте конкретні питання**:
   ```
   Користувач: Що написано про установку в документі?
   Користувач: Які технічні характеристики зазначені?
   Користувач: Знайди інформацію про вимоги до системи
   ```
3. **Перевірте джерела**: Бот має вказувати назву файлу та сторінку
4. **Тест пам'яті + RAG**:
   ```
   Користувач: Моє ім'я Іван
   Бот: Привіт, Іван!

   Користувач: Що сказано про ціни в документі?
   Бот: [знаходить інформацію в PDF]

   Користувач: Як мене звати і яка ціна?
   Бот: Тебе звати Іван, і згідно з документом... [комбінує пам'ять + RAG]
   ```

## 📁 Управління даними

### Розташування файлів

**Векторна база документів:**
```
~/.local/share/crewai-chatbot/rag_documents/
```

**Пам'ять розмов:**
```
~/.local/share/crewai-chatbot/
```

### Очищення даних

**Видалити всі документи:**
```bash
rm -rf ~/.local/share/crewai-chatbot/rag_documents/*
```

**Очистити пам'ять розмов:**
```bash
rm -rf ~/.local/share/crewai-chatbot/*
```

Або використовуйте кнопки в інтерфейсі.

## 💡 Поради для роботи з RAG

1. **Якість документів**: RAG працює краще з текстовими PDF (не скановані зображення)
2. **Розмір chunk'ів**: За замовчуванням 1000 символів. Можна змінити в `PDFProcessor`
3. **Кількість результатів**: За замовчуванням повертається 4 найрелевантніші фрагменти
4. **Конкретні питання**: Формулюйте питання чітко для кращих результатів
5. **Багато документів**: Можна завантажувати необмежену кількість PDF файлів

## 🔬 Векторні Сховища та Оцінка

### Підтримувані Vector Stores

**ChromaDB (За замовчуванням)**
- Повнофункціональне, з підтримкою metadata filtering
- Легке видалення документів
- Ідеально для production
- Швидкість: ~40ms пошуку

**FAISS (Facebook AI Similarity Search)**
- Екстремально швидкий пошук
- Оптимізований для великих датасетів (100K+ документів)
- Швидкість: ~15ms пошуку (в 3 рази швидше)
- Ефективніше використання пам'яті

**Вибір vector store:**
```python
from utils import create_vector_store

# ChromaDB (default)
vector_store = create_vector_store("chromadb")

# FAISS (для швидкості)
vector_store = create_vector_store("faiss")
```

**Детальне порівняння:** Дивіться [VECTOR_STORES_COMPARISON.md](VECTOR_STORES_COMPARISON.md)

### Інструменти Оцінки

**Швидке порівняння продуктивності:**
```bash
python compare_vector_stores.py
```
Вимірює швидкість завантаження, пошуку та релевантність

**Повна оцінка з Ragas metrics:**
```bash
python evaluate_rag.py
```
Оцінює якість RAG за метриками:
- Faithfulness (точність відповідей)
- Answer Relevancy (релевантність відповідей)
- Context Precision (точність контексту)
- Context Recall (повнота контексту)
- Context Relevancy (релевантність контексту)

**Повне тестування на Kubernetes документації:**
```bash
python test_rag_kubernetes.py
```
Порівнює ChromaDB vs FAISS на 40 Kubernetes запитаннях:
- Автоматичне завантаження PDF з `data/pdf/`
- Налаштування chunk_size, top_k, alpha
- Консоль: час відповіді, similarity scores, summary
- JSON: детальні результати в `test_results/`
- Порівняння швидкості та якості відповідей

**Використання:**
1. Додайте Kubernetes PDF документи у `data/pdf/`
2. Запустіть `python test_rag_kubernetes.py`
3. Результати у консолі та JSON файлі

**Детальна документація:** [RAG_PROCESSING.md](RAG_PROCESSING.md)

## 🔮 Майбутній розвиток

- [x] **RAG для роботи з документами** ✅ **ЗАВЕРШЕНО**
- [x] **Множинні vector stores (ChromaDB + FAISS)** ✅ **ЗАВЕРШЕНО**
- [x] **RAG evaluation з ragas metrics** ✅ **ЗАВЕРШЕНО**
- [x] **Kubernetes RAG testing framework** ✅ **ЗАВЕРШЕНО**
- [ ] Hybrid search (комбінування ChromaDB + FAISS)
- [ ] FAISS GPU підтримка
- [ ] Підтримка інших форматів (DOCX, TXT, MD)
- [ ] OCR для сканованих PDF
- [ ] Orchestrator Agent для координації multiple agents
- [ ] Image Generation Agent (DALL-E / Stable Diffusion)
- [ ] Audio Response Agent (Text-to-Speech)
- [ ] Multi-user підтримка з окремими сесіями
- [ ] Advanced reranking з cross-encoder models

## 🤝 Внесок

Це навчальний проєкт. Не соромтесь форкати та експериментувати!

## 📄 Ліцензія

MIT License

## 🙏 Подяки

- [CrewAI](https://github.com/joaomdmoura/crewAI) за чудовий framework
- [OpenAI](https://openai.com) за API
- [Streamlit](https://streamlit.io) за простий веб-фреймворк
