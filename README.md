# Чат-бот на базі CrewAI + Claude

Навчальний проект чат-бота з короткостроковою пам'яттю на базі CrewAI та Claude API.

## 🚀 Швидкий старт

### 1. Встановлення залежностей

```bash
pip install -r requirements.txt
```

### 2. Налаштування змінних оточення

Створіть файл `.env` на основі `.env.example`:

```bash
cp .env.example .env
```

Додайте ваш API ключ від Anthropic:

```
ANTHROPIC_API_KEY=your_actual_api_key_here
```

Отримати API ключ можна тут: https://console.anthropic.com/

### 3. Запуск

```bash
python main.py
```

## 📁 Структура проєкту

```
chatbot-project/
├── .env                          # Змінні оточення (не комітити!)
├── .env.example                  # Шаблон для змінних оточення
├── requirements.txt              # Python залежності
├── main.py                       # Точка входу
├── agents/
│   └── conversation_agent.py     # Conversation Agent
└── crew/
    └── chat_crew.py              # Координатор агентів
```

## 🎯 Функціонал

### Поточні можливості:
- ✅ Базова текстова розмова з Claude
- ✅ Короткострокова пам'ять (контекст розмови)
- ✅ Природне спілкування українською мовою
- ✅ Команди для управління (clear, exit)

### Заплановані можливості:
- 🔄 Orchestrator Agent (координатор агентів)
- 🔄 Image Generation Agent (генерація зображень)
- 🔄 Audio Response Agent (аудіо відповіді)
- 🔄 RAG Agent (робота з базою знань)

## 💡 Використання

### Базове спілкування:

```
👤 Ви: Привіт! Як справи?
🤖 Бот: Привіт! У мене все чудово, дякую...
```

### Команди:

- `clear` або `reset` - очистити пам'ять розмови
- `exit` або `quit` - вийти з програми

### Контекстна пам'ять:

Бот запам'ятовує контекст розмови:

```
👤 Ви: Мене звати Іван
🤖 Бот: Приємно познайомитися, Іване!

👤 Ви: Як мене звати?
🤖 Бот: Вас звати Іван, ви мені щойно представилися.
```

## ⚙️ Налаштування

### Зміна моделі Claude:

У файлі `.env`:

```
MODEL_NAME=claude-sonnet-4-5-20250929
```

### Налаштування температури (креативність):

```
TEMPERATURE=0.7  # 0.0 (точність) до 1.0 (креативність)
```

### Максимальна довжина відповіді:

```
MAX_TOKENS=4096
```

## 🧪 Тестування компонентів

### Тест Conversation Agent:

```bash
python agents/conversation_agent.py
```

### Тест Chat Crew:

```bash
python crew/chat_crew.py
```

## 📚 Технології

- **CrewAI** - фреймворк для multi-agent систем
- **Claude (Anthropic)** - Large Language Model
- **LangChain** - бібліотека для роботи з LLM
- **Python 3.11+** - мова програмування

## 🔧 GitHub Codespaces

Проект готовий для запуску в GitHub Codespaces. Після відкриття:

1. Створіть `.env` файл з вашим API ключем
2. Встановіть залежності: `pip install -r requirements.txt`
3. Запустіть: `python main.py`

## 📖 Наступні кроки

1. **Додати Orchestrator Agent** - для розумного вибору між агентами
2. **Інтегрувати Image Generation** - DALL-E або Stable Diffusion
3. **Додати Audio Agent** - OpenAI TTS для голосових відповідей
4. **Реалізувати RAG** - векторна база даних для знань
5. **Створити веб-інтерфейс** - FastAPI + React

## 🤝 Навчальні цілі

- ✅ Розуміння архітектури multi-agent систем
- ✅ Робота з Claude API через CrewAI
- ✅ Управління контекстом та пам'яттю в AI системах
- 🔄 Координація між різними типами AI агентів
- 🔄 Інтеграція різних AI сервісів (текст, зображення, аудіо)

## 📝 Ліцензія

Навчальний проект - вільне використання
